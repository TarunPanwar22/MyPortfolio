[build]
  command = "npm install && npm run build"
  publish = "build"

[build.environment]
  NODE_ENV = "production"
  CI = "false"
  SECRETS_SCAN_ENABLED = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200